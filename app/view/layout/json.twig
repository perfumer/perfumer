{
    "status": "{{ status|default(0) }}",
    "message": "{{ message|default('') }}",
    "content": {% if content is iterable %}{{ content|default([])|json_encode|raw }}{% else %}"{{ content|default('') }}"{% endif %},
    "errors": {{ errors|default([])|json_encode|raw }}
}